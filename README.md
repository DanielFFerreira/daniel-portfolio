# Portfólio (Vue 3 + TypeScript)

Projeto de portfólio em **Vue 3**, **TypeScript** e **Vue Router**, mantendo o **CSS original** do template, com melhorias de organização e **Dark Mode**.

## Rodar localmente

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```

## Dark Mode

Use o botão (🌙/☀️) no topo para alternar tema. A preferência fica salva no `localStorage`.
